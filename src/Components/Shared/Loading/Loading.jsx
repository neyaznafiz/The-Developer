import React from 'react';
import loading from '../../../Images/loading.gif'

const Loading = () => {
    return (
        <div>
            <img src={loading} alt=""  className='h-32 mt-20'/>
    
  
        </div>
    );
};

export default Loading;