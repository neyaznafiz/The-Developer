import React from 'react';
import loading from '../../../Images/loading.gif'

const Loading = () => {
    return (
        <div>
            <img src={loading} alt="" />
    
  
        </div>
    );
};

export default Loading;